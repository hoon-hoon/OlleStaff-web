import Menu from "../components/Menu";

const LoginPage = () => {
    return (
        <div>
            <h1>LoginPage</h1>
            <Menu version="guesthouse" />
        </div>
    );
};

export default LoginPage;
